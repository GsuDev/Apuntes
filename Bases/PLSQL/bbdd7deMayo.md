```sql
CREATE TABLE PIEZAS (
    COD_PIEZA NUMBER(10) PRIMARY KEY,
    NOMBRE VARCHAR2(30),
    PRECIO NUMBER(4,2),
    TIPO VARCHAR2(10),
    FECHA DATE
);

CREATE TABLE PIEZAS_AUDIT(
    COD_PIEZA NUMBER(10),
    PRECIO_VIEJO NUMBER(4,2),
    PRECIO_NUEVO NUMBER(4,2),
    USUARIO VARCHAR2(50),
    FECHA DATE,
    PRIMARY KEY (COD_PIEZA, FECHA)
);

INSERT INTO PIEZAS VALUES(1,'TORNILLO',2,'FERRETERIA', SYSDATE);
INSERT INTO PIEZAS VALUES(2,'TUERCA',3,'FERRETERIA', SYSDATE);
INSERT INTO PIEZAS VALUES(3,'MESA',20,'MOBILIARIO', SYSDATE);
INSERT INTO PIEZAS VALUES(4,'SILLA',10,'MOBILIARIO', SYSDATE);
INSERT INTO PIEZAS VALUES(5,'CAMA',80,'MOBILIARIO', SYSDATE);
INSERT INTO PIEZAS VALUES(6,'ALAMBRE',1,'FERRETERIA', SYSDATE);
```